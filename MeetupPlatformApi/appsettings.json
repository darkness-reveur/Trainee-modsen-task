{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "ConnectionStrings": {
    "DbConnectionString": "Host=<host>;Port=<port>;Database=<db-name>;Username=<userName>;Password=<password>"
  }
}
